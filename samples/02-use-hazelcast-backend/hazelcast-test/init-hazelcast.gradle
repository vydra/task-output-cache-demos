initscript {
	repositories {
		maven { url "http://repo.gradle.org/gradle/repo/" }
	    mavenCentral()
	}

	dependencies {
	    classpath "org.gradle.caching.hazelcast:gradle-hazelcast-plugin:0.5"
	}
}

apply plugin: org.gradle.caching.hazelcast.HazelcastPlugin
