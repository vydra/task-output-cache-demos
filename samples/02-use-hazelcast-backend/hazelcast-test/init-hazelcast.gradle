initscript {
	repositories {
		maven { url "http://repo.gradle.org/gradle/repo/" }
	    mavenCentral()
	}

	dependencies {
	    classpath "org.gradle.cache.tasks.hazelcast:gradle-hazelcast-plugin:0.2"
	}
}

apply plugin: org.gradle.cache.tasks.hazelcast.HazelcastPlugin
